metadata:
  name: frontend
component: frontend
labels:
  appName: frontend
  project_name: expense
selector:
  appName: frontend
  project_name: expense
initContainers:
  enabled: false
containers:
  cont_imageVersion: f1
  name: frontend
service:
  type: LoadBalancer
ports:
  protocol: TCP
  port: 80
  targetPort: 80