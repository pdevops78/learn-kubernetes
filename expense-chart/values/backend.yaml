metadata:
  name: backend
labels:
  appName: backend
initContainers:
  imageVersion: p2
  enabled: true
schema:
  schema_imageVersion: s7
containers:
  cont_imageVersion: p1
project_name: expense
component: backend
service:
  type: ClusterIP
ports:
  protocol: TCP
  port: 80
  targetPort: 8080


